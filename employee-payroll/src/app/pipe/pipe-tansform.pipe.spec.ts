import { PipeTansformPipe } from './pipe-tansform.pipe';

describe('PipeTansformPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeTansformPipe();
    expect(pipe).toBeTruthy();
  });
});
